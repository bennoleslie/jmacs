README

You need to install node to run jmacs.

$ node server.js

Open the browser at http://localhost:9090/

Right now you can only edit the README file :)

Can show two-up, but can only edit the left panel right now.

Use F1, F2 and F3 to change to the preset layouts


TODO:

Open support.
Multiple buffer support.
Faster redraw.
vi key codes.
Handle long lines
undo / redo support
Scroll bars
